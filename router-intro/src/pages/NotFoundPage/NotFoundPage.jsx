import { ContentSection } from "../../components/ContentSection/ContentSection"

export function NotFoundPage() {
    return (
        <ContentSection pageTitle='Not found'>
            <p>Welcome</p>
            <p>This is the not found page</p>
        </ContentSection>
    )
}